# Things to add in future or proposals

- [ ] **Composer** for autoloading (`vendor/`, PSR-4) and dependencies.
Include `composer.json` in the repo; Then, load `vendor/autoload.php` from `src/bootstrap.php`.

